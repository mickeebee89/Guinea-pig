/**
 * The "Example screen" label for demo-mode screenshots. In every real build
 * this renders nothing.
 *
 * Under local `next dev` with DEMO_MODE=1, next.config.ts aliases this module
 * to lib/demo/DemoLabel.tsx. Nowhere else can it show. Audit item 69.
 */
export function DemoLabel() {
  return null
}
